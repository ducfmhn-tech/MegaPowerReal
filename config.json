CFG = {
    # Project Info
    "project_name": "Mega-Power (real)",
    
    # Email settings (read from GitHub Secrets when running Actions)
    "email_sender": None,     # Will be filled from ENV
    "email_receiver": None,   # Will be filled from ENV
    "email_password": None,   # Gmail App Password (ENV)
    "email_subject": "Mega-Power (real) — Daily Report",

    # Data
    "n_periods": 100,         # fetch last 100 draws
    "window": 50,             # ML feature window size
    "threshold_retrain_pct": 50,
    "mega_max": 45,
    "power_max": 55,

    # Directories
    "data_dir": "data",
    "models_dir": "models",
    "reports_dir": "reports",

    # URLs — official Vietlott history
    "mega_url": "https://www.ketquadientoan.com/tat-ca-ky-xo-so-mega-6-45.html",
    "power_url": "https://www.ketquadientoan.com/tat-ca-ky-xo-so-power-655.html",
}

